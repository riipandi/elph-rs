//! Terminal UI components for Elph agent.
//!
//! ans: elph-tui is temporarily disabled. The TUI implementation lives directly
//! in the `elph` binary crate while we iterate on the tuie-based shell. Once the
//! public API stabilises, the reusable widget library will be extracted back into
//! this crate and published to crates.io.
//!
//! See `elph/src/tui.rs` for the current TUI implementation.
